# 基础设置
mixed-port: 7890
allow-lan: true
unified-delay: true
tcp-concurrent: true
ipv6: true
bind-address: '*'
mode: rule
log-level: info
external-controller: '127.0.0.1:9090'

# DNS 设置（开启 fake-ip，绕过 DNS 污染）
dns:
  enable: true
  ipv6: true
  default-nameserver: [223.5.5.5, 119.29.29.29]  # 国内 DNS 作为初始解析
  enhanced-mode: fake-ip  # 使用 fake-ip 模式，确保 DNS 安全
  fake-ip-range: 198.18.0.1/16
  use-hosts: true
  nameserver: ['https://dns.alidns.com/dns-query', 'https://doh.pub/dns-query']
  fallback: ['https://8.8.8.8/dns-query', 'https://1.1.1.1/dns-query']
  nameserver-policy:
    'anaconda.com': [223.5.5.5, 119.29.29.29]  # 未屏蔽网站使用国内 DNS
    'python.org': [223.5.5.5, 119.29.29.29]
    'pypi.org': [223.5.5.5, 119.29.29.29]
  fake-ip-filter:
    - '*.lan'
    - '*.local'
    - 'localhost'
    - '127.*'
    - '192.168.*'
    - '10.*'
    - '*.cn'  # 过滤国内域名，减少 fake-ip 干扰
    - '*.com.cn'
  fallback-filter: { geoip: true, geoip-code: CN }

# 代理节点
proxies:
  - name: "V2RAYN_ENGINE"
    type: socks5
    server: 127.0.0.1
    port: 10808
  - name: "VPN_ENGINE"
    type: socks5
    server: 127.0.0.1
    port: 9909

# 代理组
proxy-groups:
  - name: "AI-SPECIAL-PROXY"
    type: select
    proxies:
      - "VPN_ENGINE"
      - "V2RAYN_ENGINE"
  - name: "PROXY"
    type: select
    proxies:
      - "V2RAYN_ENGINE"
      - "VPN_ENGINE"

# Rule Providers（使用 Loyalsoldier/clash-rules）
rule-providers:
  reject:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/reject.txt"
    path: ./ruleset/reject.yaml
    interval: 86400
  icloud:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/icloud.txt"
    path: ./ruleset/icloud.yaml
    interval: 86400
  apple:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/apple.txt"
    path: ./ruleset/apple.yaml
    interval: 86400
  google:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/google.txt"
    path: ./ruleset/google.yaml
    interval: 86400
  proxy:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/proxy.txt"
    path: ./ruleset/proxy.yaml
    interval: 86400
  direct:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/direct.txt"
    path: ./ruleset/direct.yaml
    interval: 86400
  private:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/private.txt"
    path: ./ruleset/private.yaml
    interval: 86400
  gfw:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/gfw.txt"
    path: ./ruleset/gfw.yaml
    interval: 86400
  telegramcidr:
    type: http
    behavior: ipcidr
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/telegramcidr.txt"
    path: ./ruleset/telegramcidr.yaml
    interval: 86400
  cncidr:
    type: http
    behavior: ipcidr
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/cncidr.txt"
    path: ./ruleset/cncidr.yaml
    interval: 86400
  lancidr:
    type: http
    behavior: ipcidr
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/lancidr.txt"
    path: ./ruleset/lancidr.yaml
    interval: 86400
  applications:
    type: http
    behavior: classical
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/applications.txt"
    path: ./ruleset/applications.yaml
    interval: 86400

# 规则（白名单模式，默认代理）
rules:
  # 自定义规则：AI 相关网站走 AI-SPECIAL-PROXY
  - DOMAIN-KEYWORD,grok,AI-SPECIAL-PROXY
  - DOMAIN-KEYWORD,openai,AI-SPECIAL-PROXY
  - DOMAIN-KEYWORD,claude,AI-SPECIAL-PROXY
  - DOMAIN-KEYWORD,aistudio,AI-SPECIAL-PROXY
  - DOMAIN-KEYWORD,makersuite,AI-SPECIAL-PROXY
  # 自定义规则：未屏蔽国外网站直连
  - DOMAIN-SUFFIX,anaconda.com,DIRECT
  - DOMAIN-SUFFIX,python.org,DIRECT
  - DOMAIN-SUFFIX,pypi.org,DIRECT
  - DOMAIN-SUFFIX,numpy.org,DIRECT
  # Loyalsoldier 规则集
  - RULE-SET,applications,DIRECT
  - DOMAIN,clash.razord.top,DIRECT
  - DOMAIN,yacd.haishan.me,DIRECT
  - RULE-SET,private,DIRECT
  - RULE-SET,reject,REJECT
  - RULE-SET,icloud,DIRECT
  - RULE-SET,apple,DIRECT
  - RULE-SET,google,PROXY
  - RULE-SET,proxy,PROXY
  - RULE-SET,direct,DIRECT
  - RULE-SET,lancidr,DIRECT
  - RULE-SET,cncidr,DIRECT
  - RULE-SET,telegramcidr,PROXY
  - GEOIP,LAN,DIRECT
  - GEOIP,CN,DIRECT
  - MATCH,PROXY  # 未匹配的流量走代理，保障安全